using UnityEngine;
using System.Collections;

public class Win : MonoBehaviour {

	// Use this for initialization
	void Start () {
       /* if (ScoreManager.instance.isNewHighScore())
        {
            LevelManager.instance.LoadLevel("NewHighScore");
        }*/
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
